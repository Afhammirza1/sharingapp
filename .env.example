# Environment Configuration
NODE_ENV=production
PORT=3000

# Optional: Custom file size limit (in bytes)
# MAX_FILE_SIZE=10737418240

# Optional: Custom room cleanup interval (in milliseconds)
# ROOM_CLEANUP_INTERVAL=3600000

# Optional: Enable debug logging
# DEBUG=sharenear:*